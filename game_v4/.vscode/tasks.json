{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build project",
            "command": "${workspaceRoot}\\make.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
